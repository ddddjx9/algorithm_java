package sort;

public class ShellSort {

    /**
     * 简单来说，就是分组实现插入，每组元素索引间隙称为gap
     * 每轮排序后gap逐渐变小，直到gap等于1时，说明二者相邻，结束排序
     * 对插入排序的优化，让元素更快速地交换到最终位置
     *
     * @param arr 待排序的数组
     */
    public static void shellSort(int[] arr) {

    }
}
